version: '3.7' # For more information about the version, see https://docs.docker.com/compose/compose-file/compose-versioning/
services:
  crypto-monitor-server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - ${PORT}:${PORT}
    env_file:
      - .env
  crypto-monitor-client:
    build:
      context: ../client
      dockerfile: Dockerfile
    ports:
      - '5173:5173' # TODO ver como cambiar el puerto en node/vite y que no sea estático. Una vez hecho esto, poner ${PORT}:${PORT} y pasar PORT=puerto_deseado en el ../client/.env
    env_file:
      - ../client/.env